# secrets.local.json
# -------------
# Per-user secrets that aren't broker credentials.
# Copy to secrets.local.json and fill in.
# secrets.local.json is in .gitignore; never commit or share.

{
  "cas_pdf_password": "your_cas_password_here"
}
